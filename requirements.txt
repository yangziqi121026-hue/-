# a_share_agent_v2 —— 只读研究依赖（基础版）
# 仅公开数据 + 计算 + 报告/看板，绝不包含任何券商/交易/下单 SDK。

# 数据源（公开行情/财务，只读）
akshare>=1.12

# 计算
pandas>=2.0
numpy>=1.24

# 配置
python-dotenv>=1.0

# 回测净值图表（只读绘图，Agg 后端）
matplotlib>=3.7

# 看板（轻量，按需启用）
# streamlit>=1.30
# plotly>=5.20
